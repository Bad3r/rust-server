# Negative values cannot be launch arguments: Facepunch.CommandLine parses "-1" as a switch of its own and
# replays the orphaned +app.port as "app.port 1". -1 skips the Rust+ companion server entirely.
app.port -1
